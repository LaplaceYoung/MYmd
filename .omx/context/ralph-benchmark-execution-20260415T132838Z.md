# Ralph Context Snapshot

- Task statement:
  - Start sustained benchmark-driven execution from `docs/benchmark-execution-plan-2026-04-15.md` and commit progress continuously.
- Desired outcome:
  - Execute safe, reversible upgrade slices that preserve the current theme/aesthetics/usability while closing priority benchmark gaps.
- Known facts / evidence:
  - The approved execution plan exists at `docs/benchmark-execution-plan-2026-04-15.md`.
  - The repo is dirty with many unrelated user changes; commits must stay narrowly scoped.
  - Previously implemented baseline improvements in this repo include nested workspace indexing fixes, tab double-click rename, and TOC sync on tab switch.
  - The first imported benchmark tasks include preview/edit isolation tests, command/plugin boundary work, TOC hierarchy enhancement, rich paste pipeline, Mermaid fallback/export, and multi-window autosave protection.
- Constraints:
  - Do not regress session recovery, workspace indexing, TOC/tab sync, rename/link rewrite, or open/save/export baseline.
  - Preserve current visual theme and interaction character.
  - Use narrowly scoped commits only.
  - Follow TDD for implementation slices.
- Unknowns / open questions:
  - Whether preview/edit isolation already holds under interaction, or still needs explicit guardrails.
  - Which existing tests cover split preview behavior well enough to extend.
  - Whether any current dirty files overlap with the first safe execution slice.
- Likely codebase touchpoints:
  - `src/components/Editor/EditorContainer.tsx`
  - `src/components/Editor/WysiwygEditor.tsx`
  - `src/styles/editor.css`
  - `tests/e2e_*.spec.ts`
  - `docs/upgrade-execution-log.md`
