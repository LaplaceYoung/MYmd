## 编码后声明 - TitleBar 修复与 Alt+Q 实现
时间：2026-02-26 11:15:00

### 1. 复用了以下既有组件
- 组件与状态: 复用我们上一个任务刚搭建好的 `setSearchVisible` 控制栏显隐。复用 `executeCommand` 来下发原有的 `undo` 和 `redo` 动作。

### 2. 遵循了以下项目约定
- 代码风格：解除了冗余的 `onClick` 原生事件并在组件卸载时解绑全局监听的 `keydown` 热键。完全符合 Hooks 模型。

### 3. 未重复造轮子的证明
- 我们并没有去额外再写一个搜索弹出框专门为了 `TitleBar` 按钮，而是直接复用唤起 `SearchBar`。这避免了状态分裂。
